/**
 * AgentFleetPage - LEGACY name; kept as a thin re-export to AgentsPage
 * (V1-SPEC ┬º3.3 spec-named) for backwards source-of-truth compatibility.
 * Routing now points at AgentsPage directly (D36).
 */
export { AgentsPage as AgentFleetPage } from './AgentsPage';
