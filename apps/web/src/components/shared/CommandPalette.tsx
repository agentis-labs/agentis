/**
 * CommandPalette — V1-SPEC §3.3 spec-named entry point.
 *
 * The actual implementation already lives in
 * `components/CommandPalette.tsx`; the spec lists it under
 * `components/shared/`. This shim re-exports so the path matches §3.3.
 */

export { CommandPalette } from '../CommandPalette';
